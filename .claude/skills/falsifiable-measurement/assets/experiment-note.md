# Experiment — <the question, in one line>

Written before the measurement.

## The research question

**<To what extent / how / what>** does <the change under test> <do what to the
variable> over <the units, and how many> in <revision, fixture, date>?

| Element | This question's |
| --- | --- |
| Interrogative phrase | |
| Variable, the counter that moves | |
| Population, unit of analysis | |
| Space and time | |

**FINER** — Feasible: · Interesting, the decision that turns on it: · Novel, and
what the tool already reports: · Ethical, the fixture that can be destroyed: ·
Relevant, what acts on the answer:

**PICOT** — P: · I: · **C, the control**: · **O, the exact counter**: · T:

If no decision depends on the answer, do not run it.

## Method

The fixture and the tool, both throwaway copies rather than a checkout with work
in them, and how their identity is confirmed. The control: what is removed so
that a broken harness can be told apart from a broken design. How rounds are
ordered and which one is discarded.

## Hypotheses, and what kills each one

Each one is a tentative answer to the question above. Anything that answers
something else is a task; it goes on the task list.

**H1 — <the claim in one sentence>.** <numeric prediction>.
*Falsified if <the exact result that kills it>.*

**H2 — <…>.** <numeric prediction>.
*Falsified if <…>.*

**What would refute all of them:** <the outcome that sends this back to a new
conjecture>. If none can be named, this set enumerates rather than conjectures —
rewrite it.

## Decision rule, fixed in advance

- <outcome> → <what is done>
- <outcome> → <what is done>, including the outcome that closes the work entirely.

Nothing here belongs inside a hypothesis.

## Results

Raw per-round numbers. Exact counters and wall clock in separate columns; the
counters are what may be gated, the clock is reported only.

| Round | … | … | ratio |
| --- | --- | --- | --- |

### Controls

Each control declared above, with the evidence that it ran and what it showed.

### H1 <refuted / corroborated>

The measured numbers against the prediction. If it died, say so first, then why
the original claim was wrong.

## Verdicts: <n> of <n>

Nothing is concluded below that count. A hypothesis that became undecidable gets
a recorded verdict here, never a silent skip.

## Conclusion

Only once the count above is complete. A hypothesis that survived is corroborated,
not proven. If every one was refuted, say so and return to the question.

## What this does NOT establish

The limits, named explicitly: what was not covered, which probe was approximate,
and which question this result is silent about.
